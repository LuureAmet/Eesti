# BUILD PROMPT — Uudised (uudised)
> Paste with SITE_SPEC.md into a capable model, or read as a human builder. Rules: docs/AI_CORE_RULES.md + docs/EHITAJALE.md.

## Soul (one line, do not dilute)
The river-gatherer: write a story once (`news:pere`), it appears everywhere it belongs — news as shared context rows, never as a walled feed.

## Betrayal test
- Becoming an algorithmic feed that ranks for engagement instead of showing rivers plainly.
- Duplicating stories per site instead of one row, many renders.

## Standing on
LIVE — news:pere works end to end (write → appears everywhere). The message envelope + context_id pattern is the entire mechanism; no site-specific backend exists or should.

## Next honest step (one thing)
Run DELEGATE_PROMPTS Task 1 for uudised (it is in the queue since 07-19): produce its content.json — title, taglines, myth footer, labels. Cheap model work; paste result to lehed/uudised/content.json.

## Best-day vision
Every site gets its own river (`lood:krattiina`, `lood:ramm` — EDASISED_SEEMNED §6: new context name, zero new systems) and uudised becomes the collector showing many rivers with "näita veel" layering. Readers follow rivers across portals without leaving the plain-HTML skeleton; an RSS-like static fallback keeps it readable by any parser — the no-JS rule as a feature, not a compromise.

## Seeds (designed, NOT built)
- Per-site rivers `lood:<site>` (trigger: a second site has stories to tell).
- River subscription as a rule row per identity (trigger: first reader asks to follow).
- Weekly digest deposited as krattiina shard (trigger: rivers exist for a month).

## Guardrails specific to this site
Chronology is honest — no reordering beyond time and explicit layers. A correction is a new row referencing the old, never an edit (append-only).
