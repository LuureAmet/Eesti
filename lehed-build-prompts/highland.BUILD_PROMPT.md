# BUILD PROMPT — Highlander Pub (highland)
> Paste with SITE_SPEC.md into a capable model, or read as a human builder. Rules: docs/AI_CORE_RULES.md + docs/EHITAJALE.md.

## Soul (one line, do not dilute)
A real pub's real community hub (highlanderpub.ee) — the cluster's first site serving an existing physical place and its people, not an idea.

## Betrayal test
- Generic restaurant-template look (stock heroes, fake reviews) — this pub exists; invented content about it is a lie on its own page.
- Building ahead of the owner: PLAAN's trigger is explicit — "kui margus toob sisu" (when Margus brings the real content).

## Standing on
Live-ish page, content.json + skin.css + index.html present. Chat rooms (_shared/room.js) fit a pub naturally.

## Next honest step (one thing)
Prepare the content VESSEL, not the content: extend content.json with an empty-but-shaped structure for the real pub facts — `{hours:{}, address:"", story_et:"", story_en:"", events:[], regulars_wall:[]}` — with a one-line comment per field saying what Margus should bring. The page renders gracefully with fields missing ("tuleb" markers). Nothing invented.

## Best-day vision
The pub's regulars have instant identities, a `page:highland` room that feels like the corner table, an events river (`lood:highland`), and a wall the community actually maintains. Visiting the site feels like the place. Becomes the model for every future real-place site: vessel first, truth only, community owns the voice.

## Seeds (designed, NOT built)
- Event announcements → uudised river (trigger: first real event posted).
- Simple table-booking stub as visible 501 "tuleb" button (trigger: owner asks).
- Photo wall via URL+grant pattern, no blobs (trigger: community sends photos).

## Guardrails specific to this site
Everything on the page must be true of the real pub or clearly marked "tuleb." The pub's name and brand belong to the pub — the site serves it, never speaks over it.
