# BUILD PROMPT — Teemad (teemad)
> Read docs/AI_CORE_RULES.md + docs/EHITAJALE.md + the SITES.md "TEEMAD & REVELATSIOONID" section (this folder's spec lives there). Not a portal — a domain-based content aggregator across all pages.

## Soul (one line, do not dilute)
Eight domains × eight pages (D36): the cluster's knowledge organized by THEME (avatud valimised, info-sõda, ai-stack, ajalooline mälu, teadvuse vabadus, IT-infra, meedia-vabadus, KGB-planetaarne) so content is findable by subject, not only by portal.

## Betrayal test
- Domains drifting into opinion silos — each theme aggregates and structures; the multi-perspective work belongs to revelatsioonid.
- Breaking the 8×8 discipline by letting one hot theme sprawl while others stay empty.

## Standing on
64-page structure exists as markdown (`teemad/<teema>/<teema>_<alateema>.md`), served at /teemad/; symlinked from ~/mind/teemad. Written by Margus + HY3 (D36, 2026-07-19).

## Next honest step (one thing)
An index with honest fill-state: generate `teemad/index.html` (plain HTML from the md tree) showing all 8×8 cells with a three-state mark — written / sketch / empty — so the map shows its own gaps. A cheap-model task; no content invention, state read from the files themselves.

## Best-day vision
Any piece of cluster content (a shard, an essay, a veateade) can carry theme tags as data and surface in its theme page automatically; the 8×8 grid becomes the subject-map of everything the community knows, each cell linking portal-side deep dives and revelatsioonid perspective-sets. mind/ symlinks keep it equally readable as files and as web.

## Seeds (designed, NOT built)
- Theme tags as rule/relation rows on artifacts (trigger: two content types need cross-listing).
- Per-theme river `lood:teemad-<n>` (trigger: a theme gets living updates).
- Theme → revelatsioonid perspective-set cross-links (trigger: first theme treated by both).

## Guardrails specific to this site
Theme names and the 8-domain structure are owner decisions (D36) — extend within, don't restructure. Sensitive themes (info-sõda, KGB-planetaarne) stay documentary and source-anchored: analysis of methods and history, never operational instruction.
