# BUILD PROMPT — Hääletus (haaletus)
> Paste with SITE_SPEC.md into a capable model, or read as a human builder. Rules: docs/AI_CORE_RULES.md + docs/EHITAJALE.md.

## Soul (one line, do not dilute)
The glass ballot box: voting whose trust is mathematically demonstrable by any voter — home of ALL votings across the cluster, with counter-statistics anyone can recompute.

## Betrayal test
- Any tally a voter cannot independently verify from visible artifact chains.
- Quietly deleting or editing a cast vote (append-only truth applies doubly here).

## Standing on
LIVE (V0, proovihääletus) on core-api. Artifact chains + counter-statistics committed 07-20. aimail courier closes the loop for model participants.

## Next honest step (one thing)
V0 → V1 groundwork as DATA: define voter-role and ballot-visibility as rule rows (`site:"haaletus", layer:"default"` — who may open a voting, who may see interim counts) and propose them via DELEGATE_PROMPTS Task 4 format into docs/proposed_rules_<date>.json. No code until the rows are reviewed. Trigger per PLAAN: "kui on mida hääletada" — when there is something real to vote on.

## Best-day vision
Any portal in the cluster can spawn a voting with one context_id; voters (human or AI agent, each an identity) cast into a chain anyone can walk; results pages show the recomputation, not just the number. Becomes the trust mechanism for community decisions on every other site — linnapea's mandates, aikeskus's forum choices — voting as infrastructure, not an app.

## Seeds (designed, NOT built)
- Cross-portal voting embed (trigger: a second portal needs a real vote).
- Voter-anonymity layer via identity layers — vote provable, voter sealed (trigger: first sensitive vote).
- Result snapshots deposited to krattiina as shards (trigger: first historic vote).

## Guardrails specific to this site
Never present a count without its verification path. Rule rows for roles must be readable aloud. A failed/contested vote stays visible with its dispute — democracy's errors are data too.
