# BUILD PROMPT — Sydameke / Maikeneration (sydameke)
> Paste with SITE_SPEC.md into a capable model, or read as a human builder. Rules: docs/AI_CORE_RULES.md + docs/EHITAJALE.md. Do one step, log it, update this file's next step.

## Soul (one line, do not dilute)
Meeting people by *who they choose to be now* — age and identity as a dynamic self-presented state with an immutable choice history — not a literal age filter, not a swipe market.

## Betrayal test
- Anything that turns people into a catalog to consume (endless swiping, engagement traps).
- Softening the ≥18 rule or making visibility exceptions in code instead of rule rows (femalePoliceLayer etc. must be explicit priority rules — the spec says so).

## Standing on
LIVE since 07-16 on core-api :8090 — the cluster's flagship. Sliders, temp profiles, feed + chat, glass view (klaasvaade) all work. content.json, skin.css present. VISIBILITY_CORE thinking (per-field, asymmetric, request-to-see) was born here; IDENTITY_LAYERS.md canonizes it.

## Next honest step (one thing)
Phase B item from PLAAN: replace the prototype sliders with the real non-linear 0–120 sliders (midpoint 35) in vanilla JS — one file, no new dependencies, keep the no-JS fallback form working underneath. Verify with real clicks (Playwright), then update SITE_DEVELOPMENT.md.

## Best-day vision
The place that proves the whole identity architecture: a visitor arrives anonymous, becomes someone by choosing, reveals fields only on request with a full audit trail, and can destroy the session knowing traces are marked, not erased. Chat grows the consent-save mode (message stored only when receiver touches it) and honest WebRTC video. Other portals reuse what sydameke pioneers — it is the cluster's identity laboratory, not just a dating site.

## Seeds (designed, NOT built)
- Consent-save message chain — EDASISED_SEEMNED §1 (trigger: two real users ask for off-record talk).
- WebRTC video with STUN only — §2 (trigger: two real users).
- Auto-layer proposals from profile hashtags — §5 (trigger: 5+ people share a tag).

## Guardrails specific to this site
Age floor ≥18 is rule row id=1 — verify it stays after any rule-table work. Every visibility feature must be readable-aloud data (a moderator could explain any grant). Dignity over cleverness in all microcopy.
