# BUILD PROMPT — linnapea.ee (linnapea)
> Paste with SITE_SPEC.md into a capable model, or read as a human builder. Rules: docs/AI_CORE_RULES.md + docs/EHITAJALE.md.

## Soul (one line, do not dilute)
The city as an open repository (commit 703a007's phrase): open-source city administration — blueprints, versions, trust mandates — a mayor's office anyone can fork.

## Betrayal test
- Becoming political commentary about specific real officials — the site is about the *form* of administration, not persons.
- Closed drafts: everything here is versioned in the open or it contradicts its own name.

## Standing on
index.html + content.json + SITE_SPEC exist; commit 703a007 already framed blueprints/versions/mandates. haaletus provides the mandate-voting mechanism; artifact API holds versioned blueprints naturally.

## Next honest step (one thing)
Model one real city process as a blueprint artifact: pick something small and universal (e.g. "how a pothole report travels"), write it as `{kind:"blueprint", payload:{name, steps:[], roles:[], version:1}}` with its v2 improvement as a related artifact. Two rows, rendered statically from content.json. Shows the whole idea in one screen.

## Best-day vision
A library of administration blueprints any town could adopt: each process versioned, each improvement attributable, trust mandates granted and revoked through haaletus votes with glass-box counts. Citizens compare their city's actual process against the open blueprint and file the diff to ministeerium as a veateade. Civic infrastructure as forkable commons.

## Seeds (designed, NOT built)
- Mandate lifecycle (grant/report/revoke) as circle + rule rows + haaletus votes (trigger: first group adopts a blueprint).
- Blueprint diff view between versions (trigger: >10 blueprints).
- Municipality onboarding pack (trigger: first real municipality shows interest).

## Guardrails specific to this site
Blueprints describe roles, never name officeholders. Estonian administrative reality first (et terms with en gloss). No claim of state affiliation.
