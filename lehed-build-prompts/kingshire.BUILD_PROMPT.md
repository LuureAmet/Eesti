# BUILD PROMPT — KingShire (kingshire)
> Paste with SITE_SPEC.md into a capable model, or read as a human builder. Rules: docs/AI_CORE_RULES.md + docs/EHITAJALE.md.

## Soul (one line, do not dilute)
A realm, not a website: kingshire.ee (domain registered — one of the few real ones) — maps, culture, history, with per-section membership like chambers of a court.

## Betrayal test
- Fantasy-game aesthetics without substance — the realm frame is a way to organize real culture/history/membership, not a skin.
- Membership as paywall; restricted sections are earned/granted chambers (rule rows), never purchases.

## Standing on
index.html + content.json + skin.css + SITE_SPEC + SITE_DEVELOPMENT exist. circle + circle_member tables in core DB are exactly the chamber mechanism. Registered domain waiting to be pointed.

## Next honest step (one thing)
Model the chambers as data: propose 3 circle rows (e.g. valjak/公 open square, koda/chamber, noukoda/council) with their entry rules as rule rows — DELEGATE_PROMPTS Task 4 format, into docs/proposed_rules_<date>.json. Review before insertion. No membership code exists or is needed yet — the map of chambers comes first.

## Best-day vision
The first domain the cluster points live (it's registered — natural candidate for the HTTPS/certbot Phase C moment). Visitors walk an SVG realm map (ramm's 2.5D craft reused), each region a section, some open, some chambered; membership in a chamber is a visible, revocable grant with history. The webring `.:.` icon leads to the other portals as neighboring realms.

## Seeds (designed, NOT built)
- SVG realm map reusing ramm's mockup technique (trigger: 3 sections have real content).
- Chamber ceremonies — granted/revoked events rendered as small rituals (trigger: first real member).
- Realm chronicle → krattiina shards (trigger: first notable realm event).

## Guardrails specific to this site
When the domain points here, it's the cluster's public face first impression — the no-JS skeleton and old-device rule matter doubly. Restricted ≠ secret: the existence and rules of every chamber are public data even when contents are not.
